/**
 * Translations for the Persian strings stored in the static data files
 * (`hs-codes.ts`, `trade-services.ts`). Keyed by the exact Persian source string.
 */
export const dataTranslations: Record<string, { en: string; ar: string }> = {};
